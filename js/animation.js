  document.addEventListener("DOMContentLoaded", () => {

 AOS.init({
    once: false,        // allows re-trigger on scroll up/down
    duration: 800,      // animation duration in ms
    easing: 'ease-in-out', // optional
    delay: 100,         // optional
  });
  	
  });
